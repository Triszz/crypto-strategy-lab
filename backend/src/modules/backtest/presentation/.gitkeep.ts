/**
 * backtest · presentation
 *
 * - REST routes (`POST /api/backtests`, `GET /api/backtests/:id`,
 *   `GET /api/experiments`).
 * - DTOs (zod).
 *
 * No business logic.
 */

// TODO(backtest): add controllers + routes.
export {};