/**
 * search · application
 *
 * `SearchService`: orchestrates generator + controller, persists runs
 * through the persistence port, publishes events on the EventBus.
 *
 * Depends only on `domain/` abstractions.
 */

// TODO(search): add `SearchService`.
export {};