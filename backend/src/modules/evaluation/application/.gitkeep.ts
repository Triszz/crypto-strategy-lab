/**
 * evaluation · application
 *
 * - `EvaluationService`: subscribes to `BacktestCompleted`, runs
 *   the `Evaluator`, persists results, emits `StrategyEvaluated`.
 * - Depends only on `domain/` ports.
 */

// TODO(evaluation): add `EvaluationService`.
export {};