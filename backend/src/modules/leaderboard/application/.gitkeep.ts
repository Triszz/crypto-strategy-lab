/**
 * leaderboard · application
 *
 * - `LeaderboardService`: subscribes to `StrategyEvaluated`, recomputes
 *   Top-K, persists, emits `LeaderboardUpdated`.
 * - Depends only on `domain/` ports.
 */

// TODO(leaderboard): add `LeaderboardService`.
export {};