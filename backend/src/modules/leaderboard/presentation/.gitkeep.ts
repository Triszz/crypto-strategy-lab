/**
 * leaderboard · presentation
 *
 * - HTTP routes (`GET /api/leaderboard`, `GET /api/leaderboard/:strategyVersionId/history`).
 * - No business logic.
 */

// TODO(leaderboard): add controllers + routes.
export {};