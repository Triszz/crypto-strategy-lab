/**
 * strategy · application
 *
 * Orchestrates the domain to fulfil use cases:
 * - register a strategy
 * - evaluate a strategy on a candle window
 * - compose a composite strategy
 *
 * Rules:
 * - Depends on `domain/` abstractions.
 * - Does NOT import Prisma, Express, or external SDKs.
 *
 * Skeleton note: nothing is exported yet.
 */

// TODO(strategy): add `StrategyService` and any use-case orchestrators.
export {};