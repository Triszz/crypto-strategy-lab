/**
 * news · application
 *
 * - `NewsCollectorService`: scheduled crawl, dedupe, persist, publish.
 * - Depends on `domain/` ports + EventBus.
 */

// TODO(news): add `NewsCollectorService`.
export {};