/**
 * evaluation · domain
 *
 * - `Evaluator` port (FR-043, BR-027).
 * - `Metrics` value object and individual metric calculators
 *   (`TotalReturn`, `WinRate`, `MaxDrawdown`, `OverallScore`,
 *   `SharpeRatio`, `SortinoRatio`).
 *
 * MUST NOT import Prisma, Express, BullMQ, Socket.IO, Binance.
 */

// TODO(evaluation): add `Evaluator` and metric calculators.
export {};