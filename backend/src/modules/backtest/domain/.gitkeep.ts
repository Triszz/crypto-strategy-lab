/**
 * backtest · domain
 *
 * - `Backtester` port (FR-029).
 * - `Trade`, `Position` value objects.
 * - Domain rules for fixed position sizing and exit-after-entry
 *   (BR-022, BR-023, BR-024).
 *
 * MUST NOT import Prisma, Express, BullMQ, Socket.IO, Redis, Binance.
 */

// TODO(backtest): add `Backtester`, `Trade`, `Position`, rules.
export {};