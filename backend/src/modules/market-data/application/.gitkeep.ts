/**
 * market-data · application
 *
 * Use cases and orchestration: `MarketDataService`,
 * `BackfillUseCase`, etc.
 *
 * Rules:
 * - Depends on `domain/` abstractions.
 * - Does NOT import `@prisma/client`, Express, or any external SDK.
 *
 * Skeleton note: nothing is exported yet. The owners will add
 * concrete use-case classes in a later task.
 */

// TODO(market-data): add `MarketDataService`, `BackfillUseCase`.
export {};