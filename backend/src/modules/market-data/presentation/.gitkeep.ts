/**
 * market-data · presentation
 *
 * Express HTTP layer for the Market Data Service. Controllers must
 * be thin and must NOT contain business logic.
 *
 * Skeleton note: nothing is exported yet. Routes such as
 * `GET /api/candles` will be added by the market-data owner in a
 * later task.
 */

// TODO(market-data): add `candles.controller.ts`, `candles.routes.ts`
// and request/response DTOs (`zod` schemas).
export {};