/**
 * search · domain
 *
 * - `StrategyGenerator` interface (FR-020).
 * - `SearchController` (loop + stop condition, FR-024..FR-026, BR-018).
 * - `StopCondition` strategy.
 *
 * MUST NOT import Prisma, Express, BullMQ, Socket.IO, Redis.
 */

// TODO(search): add `StrategyGenerator`, `SearchController`,
// `StopCondition`, `RandomGenerator`, `DomainGuidedGenerator`.
export {};