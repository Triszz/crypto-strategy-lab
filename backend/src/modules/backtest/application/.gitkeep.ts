/**
 * backtest · application
 *
 * - `BacktestService`: orchestrates a Backtester for a candidate
 *   strategy, persists result, emits `BacktestCompleted`.
 * - `BacktestWorker` orchestration logic.
 *
 * Depends on `domain/` ports only.
 */

// TODO(backtest): add `BacktestService`, `BacktestWorker` orchestration.
export {};