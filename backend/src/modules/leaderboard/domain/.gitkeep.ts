/**
 * leaderboard · domain
 *
 * - `Leaderboard` aggregate (rank, overallScore, entries).
 * - Top-K computation service.
 *
 * MUST NOT import Prisma, Express, BullMQ, Socket.IO.
 */

// TODO(leaderboard): add `Leaderboard` aggregate, TopK function.
export {};