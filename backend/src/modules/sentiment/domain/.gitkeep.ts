/**
 * sentiment · domain
 *
 * - `SentimentAnalyzer` port (NFR-005).
 * - `SentimentResult` value object (Positive/Neutral/Negative,
 *   score in [-1, 1], optional confidence 0..1).
 *
 * MUST NOT import Gemini SDK, any AI SDK, Prisma, Express, BullMQ,
 * Socket.IO.
 */

// TODO(sentiment): add `SentimentAnalyzer` port, `SentimentResult`.
export {};