/**
 * search · presentation
 *
 * - HTTP routes (`POST /api/search/start`, `POST /api/search/stop`,
 *   `GET /api/search/runs`, etc.).
 * - DTOs (zod).
 *
 * No business logic; controllers delegate to `SearchService`.
 */

// TODO(search): add search routes + controllers.
export {};