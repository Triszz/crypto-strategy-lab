/**
 * strategy · presentation
 *
 * Thin Express layer:
 * - DTOs (using `zod`).
 * - Controllers that delegate to the application service.
 * - Express routers mounted under `/api/strategies`.
 *
 * Controllers MUST NOT contain business logic.
 *
 * Skeleton note: nothing is exported yet.
 */

// TODO(strategy): add `strategies.controller.ts`, `strategies.routes.ts`.
export {};