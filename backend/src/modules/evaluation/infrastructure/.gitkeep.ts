/**
 * evaluation · infrastructure
 *
 * - Prisma `BacktestResultRepository`, `EvaluationMetricRepository`,
 *   `TradeRepository`.
 * - BullMQ `evaluation` queue + worker.
 */

// TODO(evaluation): add Prisma repos + BullMQ queue/worker.
export {};