/**
 * search · infrastructure
 *
 * - Prisma `SearchRunRepository`, `CandidateStrategyRepository`,
 *   `SearchAlgorithmRepository`.
 * - BullMQ producer that enqueues backtest jobs as soon as a candidate
 *   is generated (FR-028, FR-029).
 */

// TODO(search): add Prisma repositories + BullMQ producer.
export {};