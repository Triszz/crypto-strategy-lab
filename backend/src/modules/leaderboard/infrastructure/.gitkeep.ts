/**
 * leaderboard · infrastructure
 *
 * - Prisma `LeaderboardEntryRepository`, `RankingHistoryRepository`.
 * - Optional Redis cache (`lb:top-k`, 10s TTL).
 */

// TODO(leaderboard): add Prisma repos + Redis cache.
export {};