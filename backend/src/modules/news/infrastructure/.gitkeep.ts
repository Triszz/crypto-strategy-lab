/**
 * news · infrastructure
 *
 * - `CryptoPanicAdapter` and `NewsNormalizer`.
 * - Prisma `NewsRepository`, `NewsCoinRepository`, `NewsProviderRepository`.
 */

// TODO(news): add CryptoPanic adapter, normalizer, Prisma repos.
export {};