/**
 * sentiment · application
 *
 * - `SentimentService`: listens on `NewsCollected`, runs the analyser,
 *   persists, emits `SentimentAnalyzed`.
 * - Depends on `domain/` ports and EventBus only.
 */

// TODO(sentiment): add `SentimentService`.
export {};