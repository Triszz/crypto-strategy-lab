/**
 * strategy · infrastructure
 *
 * Adapters that implement domain ports against real systems:
 * - Prisma-backed `StrategyRepository` for `strategy_definitions`,
 *   `strategy_versions`, `composite_components`, `strategy_registry`.
 * - Optional Redis cache.
 *
 * Skeleton note: nothing is exported yet.
 */

// TODO(strategy): add Prisma implementations of strategy ports.
export {};