/**
 * backtest · infrastructure
 *
 * - `BacktestQueue`: BullMQ producer for the `backtest` queue.
 * - `BacktestWorker`: BullMQ consumer (separate process or in-process).
 * - Prisma repositories for `experiments`, `trades`,
 *   `backtest_results`, `evaluation_metrics`.
 */

// TODO(backtest): add BullMQ queue/worker, Prisma repositories.
export {};