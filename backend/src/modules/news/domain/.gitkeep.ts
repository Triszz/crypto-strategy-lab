/**
 * news · domain
 *
 * - `NewsProvider` port (NFR-004, AC-07).
 * - `NewsItem` value object.
 *
 * MUST NOT import CryptoPanic or any news SDK, Express, BullMQ,
 * Socket.IO, Prisma.
 */

// TODO(news): add `NewsProvider` interface and `NewsItem` value object.
export {};